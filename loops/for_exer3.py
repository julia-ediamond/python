# Given a list, use a for loop to sum all the numbersin the list.
#random_numbers = [3, 5, 9, 1]
random_numbers = [-3, -5, 9, 1]

for result in random_numbers:
    sum(random_numbers)
print(sum(random_numbers))
